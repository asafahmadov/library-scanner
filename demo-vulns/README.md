# demo-vulns — intentionally vulnerable

This folder is a **deliberately insecure security-demo target** for apPosture
(SAST / SCA / IaC / secrets / container). Every file here contains a planted
weakness on purpose. **Do not deploy.** Secrets are AWS's public *EXAMPLE*
values and other obvious dummies — not real credentials.
